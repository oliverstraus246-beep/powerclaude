# React Hooks

Runs Prettier and ESLint on React component files (.tsx, .jsx) after every edit.

## Requirements
- Prettier: `npm install -D prettier`
- ESLint: `npm install -D eslint eslint-plugin-react`
- .eslintrc or eslint config in package.json

## Setup
Merge settings.json into ~/.claude/settings.json under "hooks".
